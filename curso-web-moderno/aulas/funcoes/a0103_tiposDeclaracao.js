console.log(soma(3, 4))
//console.log(sub(3, 4)) // Não vai funcionar
//console.log(mult(3, 4)) // Não vai funcionar



// function declaration
function soma(x, y) {
  return x + y
}

// funcion expression
const sub = function (x, y) {
  return x - y
}


// named funcion expression
const mult = function mult(x, y) {
  return x * y
}